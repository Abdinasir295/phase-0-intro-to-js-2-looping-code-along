    const names=["Charlie", "Samip", "Ali"]
    function writeCards(names, birthday) {
    return names.map(name => `Thank you, ${name}, for the wonderful ${birthday} gift!`);
  } 
  function countDown(number) {
    for (let i = number; i >= 0; i--) {
        console.log(i);
    }
}
